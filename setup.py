from setuptools import setup, find_packages

setup(
    name='Nhl fantasy predictor',
    version='0.0.1',
    description='Nhl fantasy predictor',
    packages=find_packages(),  # Automatically find all packages in your project
)